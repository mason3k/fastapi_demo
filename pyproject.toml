[project]
name = "fastapi-demo"
version = "1.0.0"
description = "A basic web api."


[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

dependencies = [
    "fastapi",
]

[project.optional-dependencies]
dev = [
  "uvicorn"
]
test = [
    "pytest"
]

